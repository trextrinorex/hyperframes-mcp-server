{
  "name": "hyperframes-mcp-server",
  "version": "1.0.1",
  "private": true,
  "description": "Remote MCP server that exposes HyperFrames video creation and rendering tools over Streamable HTTP.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "hyperframes": "0.8.33",
    "zod": "4.4.3"
  }
}
